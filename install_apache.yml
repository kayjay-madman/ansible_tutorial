---

- hosts: all
  become: true
  tasks:
    - name: Install apache & php support
      package:
        name:
          - "{{ apache_package }}"
          - "{{ php_package }}"
        state: present
        update_cache: true